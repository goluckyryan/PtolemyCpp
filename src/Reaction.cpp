// Reaction.cpp — formerly defined the global Reaction singleton; now empty
// since main() constructs a local Reaction. Kept as a TU so the Makefile
// build list stays stable. Reaction::setChannel() lives in set_channels.cpp
// and Reaction::probePrint() lives in probe_print.cpp.

#include "Reaction.h"
